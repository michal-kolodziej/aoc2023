This python script creates new package for current day using template.
default target path is hardcoded within the script.

Placeholder "{{DAY}}" is used in template, it's replaced with generated package name e.g. `day04`

USAGE: `python daygen.py ../src/`